## Example env for backend proxy
# Copy to backend/.env and set your real key (do NOT commit the real file)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
PORT=4001
# Copia este archivo a .env y rellena los valores
DATABASE_URL="mysql://user:password@localhost:3306/koxol_db"
JWT_SECRET=replace_with_a_long_random_secret
PORT=4000
# SMTP (opcional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
